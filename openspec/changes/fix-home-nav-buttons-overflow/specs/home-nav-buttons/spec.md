## Requirement: 首页底部导航按钮不溢出

### Scenario: 窄屏设备上按钮自动换行
Given 用户在窄屏手机上打开首页
When 页面底部渲染 4 个操作按钮
Then 按钮在宽度不足时自动换到下一行
And 每个按钮的完整文字都可见

### Scenario: 宽屏设备上按钮保持单行
Given 用户在宽屏手机或平板上打开首页
When 页面底部渲染 4 个操作按钮
Then 按钮在同一行显示
And 布局效果与修复前一致
